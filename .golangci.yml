linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - ineffassign
    - typecheck
    - misspell
    - goimports
linters-settings:
  goimports:
    # put imports beginning with prefix after 3rd-party package
    local-prefixes: github.com/onflow/flow-cli
